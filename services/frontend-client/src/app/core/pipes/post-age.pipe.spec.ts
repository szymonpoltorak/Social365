import { PostAgePipe } from './post-age.pipe';

describe('PostAgePipe', () => {
    it('create an instance', () => {
        const pipe = new PostAgePipe();
        expect(pipe).toBeTruthy();
    });
});
