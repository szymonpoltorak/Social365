export enum RouterPaths {
    HOME = 'home',
    HOME_DIRECT = '/home',

    CURRENT_PATH = '',

    FEED = "feed",

    UNKNOWN_PATH = "**",

    PROFILE = "profile/:username",
    PROFILE_DIRECT = "/profile",
}
