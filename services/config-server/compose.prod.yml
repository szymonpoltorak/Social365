version: "3.8"

services:
    config-server:
        container_name: config-server
        image: config-server
        build:
            context: .
            dockerfile: Dockerfile.prod
        env_file: .env
        ports:
            - "${CONFIG_SERVER_PORT}:${CONFIG_SERVER_PORT}"
        networks:
            - social365-network
